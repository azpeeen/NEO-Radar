/* NEO Radar — Three.js renderer (v2 placeholder)
   Will replace canvas2d.js for 3D heliocentric view.
   ZERO imports from src/physics/ — receives state[] arrays only.

   TODO (next sprint):
   - Import Three.js via CDN or bundler
   - WebGLRenderer with logarithmic depth buffer
   - InstancedMesh for asteroid cloud
   - Tube geometry for uncertainty cone
   - OrbitControls for camera
*/
